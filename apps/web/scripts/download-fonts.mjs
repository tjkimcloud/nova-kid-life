/**
 * download-fonts.mjs
 *
 * NOTE: Fonts are now loaded via next/font/google (Plus Jakarta Sans + DM Sans).
 * Google handles self-hosting automatically at build time — no manual download needed.
 * This script is kept as a no-op so CI doesn't break.
 */
console.log('Fonts: using next/font/google — no manual download needed.')
